apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-cert
  namespace: argocd
spec:
  dnsNames:
    - nginx.cluster.local
  secretName: argocd-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer